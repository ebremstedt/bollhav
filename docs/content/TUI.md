[Home](index.md) › **TUI**

# TUI

!!! info "Coming soon"
    A terminal UI for bollhav — browse your models, inspect per-model state and run history, and trigger runs without leaving the terminal — is in active development.

Install the extra to follow along:

```bash
pip install "bollhav[tui]"
```

Check back here for the command reference and screenshots once it ships.
