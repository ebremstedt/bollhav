---
title: "Batching 📦"
body: "Divide the model's work into smaller intervals"
---

An interval expression defines the chunk (`@hourly`, `@daily`) whose ticks become the `(since, until)` windows a model iterates.
