## [2.1.0] - 2026-05-29

Large release. Five previously-coupled or absent feature surfaces — **state, staging, library, target setup, and per-interval extensibility** — are decoupled so they can be opted into independently along orthogonal axes. The biggest concept added is the **Actions system**: every lifecycle operation (CREATE SCHEMA, CREATE TABLE, mark_running, ANALYZE, GRANT, …) is a callable in a list, and users plug in their own without monkey-patching the framework.

### Added — State: per-model interval state tables

State tracking is opt-in via `Model(state=State(...))`. When set, bollhav creates and maintains per-model state in two tables alongside the target:

| Table | Schema (default) | Contents |
|---|---|---|
| `<target_name>_state` | `z_<target_schema>` | One row per `(since, until)` interval the model says should exist. Columns: `id`, `run_id`, `since`, `until`, `status`, `blocked_reason`, `applied_at`. `(since, until)` is unique. |
| `<target_name>_errors` | `z_<target_schema>` | One row per execute exception across all runs. Columns: `id`, `run_id`, `full_name`, `error_type`, `error_message`, `traceback`, `created_at`. Joinable with the state table on `(since, until)` for per-interval inspection or on `run_id` for per-invocation lookups. |

The state schema defaults to `z_<target_schema>` so bollhav-owned tables stay out of the user's schemas. Override via `State(schema_prefix=...)` / `State(table_suffix=...)`.

The status column is one of:

| Status | Set by | Meaning |
|---|---|---|
| `pending` | bootstrap (prefill) | queued to run; the user's loop iterates these |
| `running` | `mark_running` PRE_INTERVAL action (immediately before invoking `execute`) | currently being processed — visible in live dashboards |
| `applied` | `mark_applied` POST_INTERVAL action after a clean run, **or** the staged flush in the same transaction as the data move | completed successfully |
| `blocked` | bootstrap or live re-check | an out-of-pipeline upstream isn't satisfied; reason carries a `STATE_NNN` block code |
| `error` | `@state` when execute raises (writes errors row + flips state) | full details written to the sibling `_errors` table, then re-raised; auto-retried on the next run under `STATE_MODE=discover` |

`STATE_MODE=discover` (default) preserves `applied` rows on re-evaluation and recomputes everything else against the current upstream state; `STATE_MODE=bulldozer` resets every row to the freshly-computed status (`applied_at` cleared too). `STATE_DISABLED=true` forces a pipeline to run with no state tracking even when models declare it.

Per-interval Postgres advisory locks (keyed by `(model.full_name, since, until)`) let multiple workers on the same model split intervals safely — same-interval collisions silently skip; different-interval workers don't conflict. An optional model-wide `model_lock` is still available for stricter one-pipeline-at-a-time semantics.

See [State](docs/content/STATE.md) for the full status lifecycle, re-evaluation rules, locking, and env-var reference

### Added — Pluggable Actions system

Every one-shot DDL or lifecycle hook (CREATE SCHEMA, CREATE TABLE, TRUNCATE, ADD UNIQUE, staging setup/teardown, `mark_running`, `mark_applied`, user `GRANT` / `ANALYZE` / `COMMENT`, …) is now an `Action(name, phase, run, should_run)`. The runners walk each phase's applicable actions in declared order, calling `should_run(model)` to gate, running the action, and recording it in `target._applied_model_actions[name]` so subsequent intervals short-circuit.

**Four phases form a 2×2 grid:**

| Phase | Cardinality | Use |
|---|---|---|
| `PRE_MODEL` | Once per pipeline run, before user's loop | CREATE TABLE, CREATE INDEX, staging schema setup |
| `POST_MODEL` | Once per pipeline run, after user's loop returns cleanly | ANALYZE, GRANT, drop staging |
| `PRE_INTERVAL` | Per interval, before each `execute()` | `mark_running`, per-interval metrics, lock acquire, user logging |
| `POST_INTERVAL` | Per interval, after each `execute()` returns | `mark_applied`, per-interval cleanup, Prometheus increment, downstream notify |

**Two lists on `Target`** so framework defaults and user-added actions stay separately addressable:

- `default_actions: list[Action] | None = None` — framework's; `None` means "resolve lazily from the backend's `default_actions()` factory." Set to `[]` to opt out of every framework default. Set to a filtered list to opt out selectively.
- `actions: list[Action] = []` — user-added. Always runs after defaults.
- `effective_actions` property returns `default_actions ++ actions`.

`Target.on_failure: OnFailure = FAIL_FAST` — per-target policy for POST action failures. `FAIL_FAST` halts the pipeline POST sweep; `SKIP` logs and continues. PRE is always fail-fast.

**Backend-specific defaults** live in `bollhav.postgres.actions.default_actions()`, which returns 12 actions: 8 PRE_MODEL (schema/table/recreate/truncate/indexes/uniques/staging schema+table), 2 POST_MODEL (staging truncate + drop in REUSED mode), 2 INTERVAL (`mark_running` PRE, `mark_applied` POST — both gated on `model.state is not None`).

**Public runners:**

- `run_pre_model_actions(conn, model)` — called from `write()`. Single transaction.
- `run_post_model_actions(conn, model)` — called from `@load_models` after `main()` returns cleanly. Per-action error handling per `Target.on_failure`.
- `run_pre_interval_actions(conn, model, since, until)` — called by `@state` decorator. Stashes interval window on the model as `_interval_since` / `_interval_until` for action callables.
- `run_post_interval_actions(conn, model, since, until)` — called by `@state` decorator on success.

`Action.should_run` signature is `Callable[[Model], bool]` (takes the full model, not just the target) so feature-gated actions like `mark_running` can check `m.state is not None`. `target._applied_model_actions` records both `True` (ran) and `False` (gated out by `should_run`) — `setup_complete` checks `name in dict` so it can short-circuit even when some defaults skipped.

`ensure_schema_and_table(conn, model)` and `ensure_table(conn, model)` are retained as public verb-style façades that forward to `run_pre_model_actions` — same effect, friendlier name for callers who don't want to reach into the action runner directly.

See [Actions](docs/content/ACTIONS.md) for the full lifecycle, how to add custom actions, and worked examples.

### Added — Views and library-only tables as upstreams

`Model(library=True)` opt-in — register the model in `z_bollhav.model_library` so downstreams can claim it as upstream. One mechanism, three use cases:

- Static lookup tables / externally-loaded data with no state of their own.
- VIEW models that are intended to be claimed as upstream. A view without `library=True` is still a perfectly valid bollhav model (the `CREATE OR REPLACE VIEW` runs in the user's execute), it just won't appear in the library.
- Any other state-less model that should be discoverable cross-pipeline.

The library gained a `model_type` column (`TABLE` / `VIEW`) and made `state_schema` / `state_table` nullable so view rows and library-only TABLE rows can store NULL for state pointers.

`is_satisfied(entry=...)` dispatches by entry shape: NULL state pointers ⇒ presence in the library is the satisfaction (zero SQL, no `pg_views` lookup); set state pointers ⇒ the existing applied-row encapsulation check. Block reasons name the upstream's `model_type` so operators can tell at a glance which kind they're waiting on.

`lookup` now returns a `LibraryEntry` `NamedTuple` (`upstream`, `model_type`, `state_schema`, `state_table`) instead of a positional tuple — `is_upstream_satisfied_live` and `_resolve_interval_status` updated to pass it through.

Documented in [STATE.md → Upstreams: views and library-only tables](docs/content/STATE.md#upstreams-views-and-library-only-tables) and [Library](docs/content/LIBRARY.md) with worked examples.

### Added — Staging without state, and a two-mode staging lifecycle

Staging no longer requires `state=State(...)`. Without state you keep both useful staging properties — memory-bounded chunked writes and atomic per-interval finalization (INSERT + DROP committed together) — and accept that re-runs re-process every interval because there's no `applied` gate.

`StagingMode` enum on `Staging(mode=...)` controls the staging-table lifecycle:

- `REUSED` (default) — one staging table per pipeline run. `CREATE TABLE` once (recorded in `_applied_model_actions["staging_table_created"]`), `TRUNCATE` at the start of every interval after the first, never `DROP` in flush. POST_MODEL `staging_table_truncated` + `staging_table_dropped` actions clean up after the loop. Cheapest on long backfills: ~4× less catalog churn (1 CREATE + N TRUNCATEs vs N CREATEs + N DROPs).
- `INTERVAL` — the previous behaviour. `CREATE TABLE` every interval (via `ensure_staging_table_per_interval`, not an action since per-interval CREATE doesn't fit the one-shot pattern), `DROP TABLE` inside `flush`'s tx (unless `keep_after_flush=True`). Use when you want each interval's staging artifact to be inspectable on crash.

`gc_orphan_staging_tables` is now **auto-invoked at bootstrap** for every staging model (previously defined and exported but never called). A crashed prior run's staging table is dropped on the next pipeline start, matching the long-standing docstring promise. Logs at `debug` per drop, `warning` on connection failure.

See [Staging](docs/content/STAGING.md) for the full mode comparison, atomicity story, and orphan-GC semantics.

### Added — Examples and tests

- New runnable example [examples/state_with_view](examples/state_with_view/) — `warehouse.orders` (table, state + staging) → `warehouse.v_high_value_orders` (VIEW with `library=True`) → `warehouse.high_value_sums` (table, state + staging). Walks the topo-sorted chain end-to-end and demonstrates view registration and the downstream's satisfaction-by-presence path.
- 13 E2E tests in `tests/test_e2e.py` against a real Postgres covering: state lifecycle, actions one-shot setup, both staging modes (REUSED + INTERVAL) with and without state, library register/lookup, view-as-upstream satisfied + view-without-library-true blocks, orphan staging GC at bootstrap, additive library migration.
- ~25 new unit tests in `test_library.py`, `test_staging.py`, `test_state.py`, `test_write_modes.py`. Total suite is **560 passing** at release.

### Added — Documentation

- [Actions](docs/content/ACTIONS.md) — new top-level page covering the 2×2 phase grid, the two-list Target split, all twelve framework defaults, custom action patterns (append, project bundle, replace a default), `on_failure` policy, and `_applied_model_actions` introspection.
- [Staging](docs/content/STAGING.md) — REUSED vs INTERVAL trade-offs, atomic flush, mutations integration, orphan-GC story.
- [Library](docs/content/LIBRARY.md) — registry schema, multi-image migration policy, opt-in flowchart.
- [State](docs/content/STATE.md) — Upstreams: views and library-only tables section added.
- [Target](docs/content/TARGET.md) — `actions / default_actions` and `on_failure` sections added.
- mkdocs nav, theme `logo` + `favicon` from the bollhav wave PNG.

### Changed — Schema migrations on `z_bollhav.model_library` are additive

`ensure_library` migrates older library tables with **additive, idempotent** ALTERs (sentinel-gated via `information_schema`) instead of `DROP TABLE`. The library is shared across multiple pipelines run by potentially-different bollhav images at the same time — dropping would brick concurrent old-image writers. The new shape uses `ADD COLUMN model_type TEXT NOT NULL DEFAULT 'TABLE'` (old-image inserts that omit `model_type` land as `TABLE`) and `ALTER COLUMN state_schema/state_table DROP NOT NULL` (so the new code can write NULLs for view and library-only rows). Same rule going forward for any bollhav-owned shared table: never `DROP`, never tighten `NOT NULL`.

### Changed — Validation

- Removed `Model.__init__`'s "`target.staging` requires `state=State(...)`" check. Both combinations (with state, without state) are supported.
- `model.intervals` in **backfill mode** now raises `ValueError` when `directives.until` (i.e. `BACKFILL_UNTIL`) is unset, instead of silently falling back to `latest_complete_interval()` ("today"). Backfill means a specific window — both ends must be pinned. For "to the latest complete tick" use `LATEST_ENABLED=true` (latest mode); for "to `bounds.end`" use reload mode. This was found via E2E: a programmatic caller that bypassed `@load_models` got 880 intervals instead of the 3 the model's `bounds` declared, because the silent fallback ignored `bounds.end`. Production callers driven by `@load_models` with `BACKFILL_UNTIL` set are unaffected.

### Changed — `StateMode` rename + new default

- `StateMode.RESPECT` → `StateMode.DISCOVER` (default). Same behaviour: prefill the contract intervals into the state table, preserve existing `applied` rows, re-evaluate everything else. `discover` is a clearer name for "the prefill + run-what's-left mode."
- `StateMode.DISRESPECT` → `StateMode.BULLDOZER`. Same behaviour: reset every row to the freshly-computed status, regardless of prior value (`applied_at` cleared too).
- Env var values follow: `STATE_MODE=discover|bulldozer`. Default is `discover`.

### Changed — `StagingMode.PER_INTERVAL` → `StagingMode.INTERVAL`

Just a rename for symmetry (no behavioural change). `REUSED` is still the default; `INTERVAL` is the opt-in mode that creates+drops a staging table per interval.

### Changed — Views need `library=True`, no auto-register

A `ModelType.VIEW` model no longer auto-registers in the library. Opt in via `Model(..., library=True)`, same mechanism as a state-less TABLE that wants to be claimable as upstream. A view without `library=True` is still a valid model (gets `CREATE OR REPLACE VIEW`d each run) — it just won't appear in `z_bollhav.model_library` and therefore can't be claimed as an upstream.

### Changed — Internal type hygiene

- Pyright errors across the package brought down from **26 to 3** (the remaining 3 are `Import "pyodbc" could not be resolved` in MSSQL files, only relevant if you run the MSSQL backend without pyodbc installed).
- `Model.__init__` now declares the runtime stashes (`_state_run_id`, `_state_applied_via_staging`, `_interval_since`, `_interval_until`) as typed fields so type-checkers don't complain at the mutation sites.
- `recreate_partition`, `create_replace_view` (both backends), `_apply_lookback`, `latest_complete_interval` all narrow Optionals at function entry with raised errors that explain the contract.
- `load_models` decorator now has `@overload` declarations covering both `@load_models` and `@load_models(folder=...)` call forms.

### Migration notes — for any pre-2.1.0 internal users on this repo

- Replace `target.mutations.X` reads with `target._applied_model_actions.get("X")` — same key, same boolean.
- For custom DDL that should run once per pipeline run, define an `Action(name, Phase.PRE_MODEL | Phase.POST_MODEL, run_callable)` and append to `Target(actions=[...])`. Framework defaults still fire because they live in `default_actions`.
- For per-interval hooks, define `Action(name, Phase.PRE_INTERVAL | Phase.POST_INTERVAL, run_callable)` and let the `@state` decorator's runner sweep them. Read the interval window from `model._interval_since` / `_interval_until`.
- Test fixtures that set `mutations = Mutations()` should now set `_applied_model_actions = {}`, `actions = []`, and either `default_actions = None` to trigger lazy resolution or `default_actions = default_actions()` to pin them eagerly. Pin `effective_actions` explicitly on MagicMock targets — the property doesn't compute on mocks.
